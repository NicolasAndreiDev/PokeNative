import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  card: {
    height: 100,
    width: 100,
    alignItems: 'center',
  },
  pokemonImage: {
    height: 80,
    width: 80,
  },
});

export default styles;
